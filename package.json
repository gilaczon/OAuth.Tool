{
  "name": "oauth2-client-credentials-tool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A small Vue 3 tool for exercising the OAuth2 client credentials flow and inspecting the returned token and its decoded claims.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server/index.js",
    "test": "node --test",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@vitejs/plugin-vue": "^6.0.8",
    "vite": "^8.1.5"
  }
}
